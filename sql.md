TICKA_Sagicon
ID	datetime	requerente	setor	problema	DESCRICAO	r_email resolvido

TICKA_IMPRESSORA_SCAN
ID	datetime	requerente	setor	problema	DESCRICAO	r_email resolvido

TICKA_NEW_USER
ID	datetime	requerente	setor	NOVO_FUNCIONARIO TIPO OUTRO_TIPO    SERVICOS    OUTROS COPY_USER r_email resolvido

TICKA_REMOVE_USER
ID	datetime	requerente	setor	REMOVER_FUNCIONARIO TIPO OUTRO_TIPO    SERVICOS    OUTROS   r_email resolvido


TICKA_forgot_pass
ID	datetime	requerente	setor	DESCRICAO	r_email servicos outro resolvido

TICKA_unlock_site
ID	datetime	requerente	setor	DESCRICAO	r_email   url_site resolvido

TICKA_OTHER
ID	datetime	requerente	setor	problemas DESCRICAO r_email PROBLEMAS   DESCRICAO NOVO_NOME resolvido

TICKA_MEET
ID	datetime	requerente	setor	TIPO    TEMA    data_agendamento_formatada     HORA_agendamento    OUTROS_H    DESCRICAO
CONTA_GOOGLE	r_email resolvido



CREATE TABLE TICKA_SAGICON (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    PROBLEMA VARCHAR(255),
    DESCRICAO TEXT,
    R_EMAIL VARCHAR(255),
    RESOLVIDO BOOLEAN DEFAULT FALSE
);


CREATE TABLE TICKA_IMPRESSORA_SCAN (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    PROBLEMA VARCHAR(255),
    DESCRICAO TEXT,
    R_EMAIL VARCHAR(255),
    RESOLVIDO BOOLEAN DEFAULT FALSE
);


CREATE TABLE TICKA_NEW_USER (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    NOVO_FUNCIONARIO VARCHAR(255),
    TIPO VARCHAR(100),
    OUTRO_TIPO VARCHAR(255),
    SERVICOS TEXT,
    OUTROS TEXT,
    COPY_USER VARCHAR(255),
    R_EMAIL VARCHAR(255),
    RESOLVIDO BOOLEAN DEFAULT FALSE
);


CREATE TABLE TICKA_REMOVE_USER (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    REMOVER_FUNCIONARIO VARCHAR(255),
    TIPO VARCHAR(100),
    OUTRO_TIPO VARCHAR(255),
    SERVICOS TEXT,
    OUTROS TEXT,
    R_EMAIL VARCHAR(255),
    RESOLVIDO BOOLEAN DEFAULT FALSE
);


CREATE TABLE TICKA_FORGOT_PASS (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    DESCRICAO TEXT,
    R_EMAIL VARCHAR(255),
    SERVICOS TEXT,
    OUTRO TEXT,
    RESOLVIDO BOOLEAN DEFAULT FALSE
);


CREATE TABLE TICKA_UNLOCK_SITE (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    DESCRICAO TEXT,
    R_EMAIL VARCHAR(255),
    URL_SITE VARCHAR(255),
    RESOLVIDO BOOLEAN DEFAULT FALSE
);


CREATE TABLE TICKA_OTHER (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    PROBLEMAS TEXT,
    DESCRICAO TEXT,
    R_EMAIL VARCHAR(255),
    NOVO_NOME VARCHAR(255),
    RESOLVIDO BOOLEAN DEFAULT FALSE
);


CREATE TABLE TICKA_MEET (
    ID INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
    DATA_CRIACAO DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    REQUERENTE VARCHAR(255) NOT NULL,
    SETOR VARCHAR(100),
    TIPO VARCHAR(100),
    TEMA VARCHAR(255),
    DATA_AGENDAMENTO_FORMATADA VARCHAR(10), -- Formato DD/MM/YYYY (10 caracteres)
    HORA_AGENDAMENTO VARCHAR(5),           -- Formato HH:MM (5 caracteres)
    OUTROS_H TEXT,                         -- Outros horários (do campo OUTRO_SERVICO)
    DESCRICAO TEXT,
    CONTA_GOOGLE VARCHAR(255),
    R_EMAIL VARCHAR(255),
    RESOLVIDO BOOLEAN DEFAULT FALSE
);

________________________________________________________________________
INSERT INTO TICKA_SAGICON (
    REQUERENTE, 
    SETOR, 
    PROBLEMA, 
    DESCRICAO, 
    R_EMAIL
) VALUES (
    '[NOME DO REQUERENTE]', 
    '[SETOR DE ORIGEM]', 
    '[BREVE PROBLEMA]', 
    '[DESCRICAO COMPLETA DO PROBLEMA]', 
    '[EMAIL DO SOLICITANTE]'
);

INSERT INTO TICKA_IMPRESSORA_SCAN (
    REQUERENTE, 
    SETOR, 
    PROBLEMA, 
    DESCRICAO, 
    R_EMAIL
) VALUES (
    '[NOME DO REQUERENTE]', 
    '[SETOR DE ORIGEM]', 
    '[BREVE PROBLEMA]', 
    '[DESCRICAO COMPLETA DO PROBLEMA]', 
    '[EMAIL DO SOLICITANTE]'
);

INSERT INTO TICKA_NEW_USER (
    REQUERENTE, 
    SETOR, 
    NOVO_FUNCIONARIO, 
    TIPO, 
    OUTRO_TIPO, 
    SERVICOS, 
    OUTROS, 
    COPY_USER, 
    R_EMAIL
) VALUES (
    '[NOME DO REQUERENTE]', 
    '[SETOR DE ORIGEM]', 
    '[NOME DO NOVO FUNCIONARIO]', 
    '[TIPO DE CONTA (Ex: Padrao)]', 
    '[DETALHE DE OUTRO TIPO]', 
    '[SERVICOS NECESSARIOS (Ex: ERP, VPN)]', 
    '[OUTRAS SOLICITACOES]', 
    '[USUARIO PARA COPIA DE PERMISSOES]', 
    '[EMAIL DO SOLICITANTE]'
);

INSERT INTO TICKA_REMOVE_USER (
    REQUERENTE, 
    SETOR, 
    REMOVER_FUNCIONARIO, 
    TIPO, 
    OUTRO_TIPO, 
    SERVICOS, 
    OUTROS, 
    R_EMAIL
) VALUES (
    '[NOME DO REQUERENTE]', 
    '[SETOR DE ORIGEM]', 
    '[NOME DO FUNCIONARIO A REMOVER]', 
    '[TIPO DE CONTA (Ex: Padrao)]', 
    '[DETALHE DE OUTRO TIPO]', 
    '[SERVICOS A REMOVER (Ex: ERP, VPN)]', 
    '[OUTRAS SOLICITACOES]', 
    '[EMAIL DO SOLICITANTE]'
);

INSERT INTO TICKA_FORGOT_PASS (
    REQUERENTE, 
    SETOR, 
    DESCRICAO, 
    R_EMAIL, 
    SERVICOS, 
    OUTRO
) VALUES (
    '[NOME DO REQUERENTE]', 
    '[SETOR DE ORIGEM]', 
    '[DESCRICAO DO PROBLEMA]', 
    '[EMAIL DO SOLICITANTE]', 
    '[SERVICOS ENVOLVIDOS]', 
    '[OUTRAS OBS]'
);

INSERT INTO TICKA_UNLOCK_SITE (
    REQUERENTE, 
    SETOR, 
    DESCRICAO, 
    R_EMAIL, 
    URL_SITE
) VALUES (
    '[NOME DO REQUERENTE]', 
    '[SETOR DE ORIGEM]', 
    '[MOTIVO DO DESBLOQUEIO]', 
    '[EMAIL DO SOLIITANTE]', 
    '[URL_lINK]'
);

INSERT INTO TICKA_OTHER (
    REQUERENTE, 
    SETOR, 
    PROBLEMAS, 
    DESCRICAO, 
    R_EMAIL, 
    NOVO_NOME
) VALUES (
    '[NOME DO REQUERENTE]', 
    '[SETOR DE ORIGEM]', 
    '[PROBLEMAS GERAIS]', 
    '[DESCRICAO DETALHADA]', 
    '[EMAIL DO SOLICITANTE]', 
    '[CAMPO NOVO_NOME (SE APLICAVEL)]'
);